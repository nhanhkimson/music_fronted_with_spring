# Copy to `.env.local` in this folder.

# --- Optional: use a different host for POST /api/download (e.g. Spring on port 8080) ---
# Leave unset for full-stack Next.js (same origin): Vercel runs /api/download with play-dl.
# NEXT_PUBLIC_API_URL=http://localhost:8080
