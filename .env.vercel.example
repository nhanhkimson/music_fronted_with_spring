# Vercel: you usually need NO env vars — `prebuild` bundles `bin/yt-dlp` automatically.
#
# Only if you want the browser to call a separate API instead of same-origin `/api/download`:
# NEXT_PUBLIC_API_URL=https://your-api.example.com
